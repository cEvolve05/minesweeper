import { VerticalBox, Button } from "std-widgets.slint";
import { Empty } from "@project/widget/empty.slint";
import { View } from "@project/widget/view.slint";

export global ScoreboardState {
    in-out property <int> number;
    callback set(new: int);
}

export component Scoreboard inherits View {
    VerticalBox {
        padding: 8px;
        Text {
            text: "Scoreboard" + ScoreboardState.number;
            vertical-alignment: TextVerticalAlignment.center;
            horizontal-alignment: TextHorizontalAlignment.center;
        }

        Empty {
            Button {
                text: "increase";
                clicked => {
                    ScoreboardState.set(ScoreboardState.number + 1)
                }
            }
        }
    }
}
